# Copy to terraform.tfvars and fill in (terraform.tfvars is gitignored).
# Or set in Terraform Cloud workspace as Terraform variables, or in CI as TF_VAR_*.

# Required (sensitive in TFC)
# cloudflare_api_token   = "your-api-token"
# cloudflare_account_id  = "your-cloudflare-account-id"

# Required (sensitive): Railway domain verification TXT records.
# Issued by Railway when adding a custom domain. Mark as sensitive in TFC.
# 必須（機密）: Railway カスタムドメイン追加時に発行される検証 TXT トークン。
# api_railway_verify_txt      = "railway-verify=..."
# realtime_railway_verify_txt = "railway-verify=..."

# Optional overrides
# zone_domain            = "zedi-note.app"
# api_cname_target       = "2yg7k4yt.up.railway.app"
# realtime_cname_target  = "nnkek1wf.up.railway.app"
