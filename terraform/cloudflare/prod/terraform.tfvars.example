# Copy to terraform.tfvars and fill in. Or set in Terraform Cloud / CI as TF_VAR_*.

# cloudflare_api_token   = "your-api-token"
cloudflare_account_id = "your-cloudflare-account-id"

# Optional: zone_domain = "zedi-note.app", pages_admin_subdomain = "admin"
